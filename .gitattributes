# Ensure shell scripts always use LF line endings so they run on Linux
*.sh          text eol=lf
install.sh    text eol=lf

# Also keep other text formats consistent
*.html        text
*.css         text
*.js          text
*.json        text
*.md          text
